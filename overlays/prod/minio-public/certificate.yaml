apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: minio-public-tls
spec:
  secretName: minio-public-tls
  issuerRef:
    kind: ClusterIssuer
    name: letsencrypt
  dnsNames:
    - media.cagea1.com
